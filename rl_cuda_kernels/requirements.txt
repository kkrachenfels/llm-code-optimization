# Core ML libraries
torch>=2.1.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.28.0
peft>=0.10.0
bitsandbytes>=0.43.0

# RL Training
trl>=0.8.0

# Utilities
numpy>=1.24.0
pandas>=2.0.0
pyyaml>=6.0
tqdm>=4.65.0

# Optional: Logging
# wandb>=0.16.0  # Requires Go to build from source, install prebuilt wheel if needed

# Optional: High-throughput inference
# vllm>=0.4.0

# Kernel evaluation (usually comes with torch)
triton>=2.2.0
